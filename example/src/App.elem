<script>
	let name = "worlds";
	$: nameWithSufix = `${name}!!!`;
	$: if (name != "worlds") {
		console.log("Name changed")
	}

	function test() {
		name = `[${name}]`;
	}
	window.TEST = test;
</script>

<main test="with {name}" another="test" no-value>
	<h1>Welcome <strong>{name}</strong>!</h1>
	<h1>Another <strong>{nameWithSufix}</strong></h1>
</main>
<footer>Test Footer</footer>

